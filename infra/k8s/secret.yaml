# NOTE: This is a template file. Do NOT commit actual secret values.
# Secrets are injected during deployment via GitHub Actions workflow using envsubst.
# All values below are placeholders that will be replaced with values from GitHub Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: studiodrop-secrets
  namespace: studiodrop
type: Opaque
stringData:
  DATABASE_URL: "PLACEHOLDER_DATABASE_URL"
  SESSION_SECRET: "PLACEHOLDER_SESSION_SECRET"
  STRIPE_SECRET_KEY: "PLACEHOLDER_STRIPE_SECRET_KEY"
  STRIPE_PUBLISHABLE_KEY: "PLACEHOLDER_STRIPE_PUBLISHABLE_KEY"
  ADMIN_PASSWORD: "PLACEHOLDER_ADMIN_PASSWORD"
